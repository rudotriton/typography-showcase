name: Merge to master
on: pull_request

jobs:
  merge:
    name: Merge pull requests
    runs-on: ubuntu-latest
    steps:
      - uses: pascalgn/automerge-action@v0.2.4
        env:
          GITHUB_TOKEN: 'pull requests'
