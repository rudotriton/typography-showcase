name: Merge to master
on: pull_request

jobs:
  merge:
    name: Merge pull requests
    runs-on: ubuntu-latest
    steps:
      - uses: pascalgn/automerge-action@v0.2.4
        env:
          GITHUB_TOKEN: 'f10a48d7676fa056ca789196cc94435a0f66cad0'
